//数组用法
int []array=new int[5];//表示创建一个可容纳5个int元素的数组
int []array=new int []{1,2,3,4,5};//定义初始化，即在定义的时候就赋初值
或者int []array={1,2,3,4,5};

int length=array.length;//获取数组的长度
数组属于引用类型，引用范围是0~Length,且大小不可变
这个不可变的意思是内存中的数据并不会被改变，只是数组名指向了不同的数据而已

指向变了，数组长度也会变，变成新的数组长度
